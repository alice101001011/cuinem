<div class="bg-dark text-secondary px-4 py-5 text-center">
  <div class="py-5">
    <h1 class="display-3 fw-bold text-white">Search recipes from the web</h1>
    <div class="col-lg-6 mx-auto">
      {{!-- <p class="fs-5 mb-4">Welcome to the new recipes platform! Here you can search for recipes from all over the
        web, upload your own and create your own "cookbook" with your favorite recipes.</p> --}}
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center ">

        <form action="/recipes/search" method="GET" class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3 input-group"
          role="search">

          <input type="text" name="q" class="form-control form-control-dark text-white bg-dark"
            placeholder="Search..." />

          {{!-- <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" id="mealType" name="mealType" value="Breakfast" />
            <label class="form-check-label" for="mealType">Breakfast</label>
          </div>
          <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" id="mealType" name="mealType" value="Lunch" />
            <label class="form-check-label" for="mealType">Lunch</label>
          </div>
          <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" id="mealType" name="mealType" value="Dinner" />
            <label class="form-check-label" for="mealType">Dinner</label>
          </div> --}}

          <button type="submit" class="btn btn-outline-light btn-md px-4 button-addon2">SEARCH</button>
        </form>

      </div>
    </div>
  </div>
</div>

<div class="container mt-5">

  <div class="rooms-body">
    {{!-- {{#if newRecipeData.hits}} --}}
    <div class="card-grid">
      {{#each newRecipeData.hits}}
      <div class="card">
        <img src={{this.recipe.image}} class="card-img-top" alt={{this.recipe.label}} />
        <div class="card-body">
          <h5 class="card-title">{{this.recipe.label}}</h5>

          <a href="/recipes/{{this.recipeId}}" type="button" class="btn btn-sm btn-outline-dark recipe-id">View Recipe
            Details</a>


          {{!-- view recipe url on another tab --}}
          {{!-- <a href="{{this.recipe.url}}" target="_blank" type="button"
            class="btn btn-sm btn-outline-dark recipe-id">View recipe site</a> --}}
          {{!-- view recipe url on another tab --}}

        </div>
      </div>
      {{/each}}
    </div>
    {{!-- {{else}} --}}

    {{!-- <div class="card-grid">
      {{#each communityRecipeData}}
      <div class="card">
        {{#if this.recipe.imageUrl}}
        <img src={{this.recipe.imageUrl}} class="card-img-top" alt={{this.recipe.label}} />
        {{/if}}
        <div class="card-body">
          <h5 class="card-title">{{this.recipe.label}}</h5>

          <a href="/community-recipes/{{this._id}}" type="button" class="btn btn-sm btn-outline-dark recipe-id">View
            Recipe Details</a>

        </div>
      </div>
      {{/each}}
    </div>


    {{/if}} --}}


    <a href="{{newRecipeData._links.next.href}}" type="button" class="btn btn-sm btn-outline-dark">Next page</a>
  </div>
</div>